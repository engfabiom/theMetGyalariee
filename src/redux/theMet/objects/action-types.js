const ObjectsTypes = {
    ADD_OBJECT: "themet/addObject", // to fetch a single object from the met api and add it to the list
    DROP_OBJECT: "themet/dropObject", // to drop an object from lists (search and all objects)
}

export default ObjectsTypes;